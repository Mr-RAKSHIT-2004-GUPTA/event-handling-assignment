from django.urls import path
from . import views  # make sure this import works

urlpatterns = [
    # Add your user-related URLs here later
    # Example:
    # path('profile/', views.UserProfileView.as_view(), name='user-profile'),
]
